# Backend lint + test configuration. The `[flake8]` section is the source of
# truth for the CI "Lint backend" step and matches .github/workflows/ci.yml.
[flake8]
select = E9,F63,F7,F82
max-line-length = 100
show-source = true
count = true
exclude =
    .venv,
    __pycache__,
    alembic/versions
