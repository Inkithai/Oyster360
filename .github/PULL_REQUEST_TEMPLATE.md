## Problem

<!-- What is broken or missing? Link the issue. -->

## Solution

<!-- What did you change? Keep it one concern. -->

## Tests

- [ ] Source change is paired with a test in the same commit
- [ ] `cd backend && pytest -m "not integration"` passes
- [ ] `cd backend && pytest --cov=app --cov-fail-under=60` passes
- [ ] `cd frontend && npm run test:coverage` passes
- [ ] `cd frontend && npm run lint && npm run typecheck` passes

## Risk

<!-- Tenant isolation, billing, auth, or data migration notes. -->
