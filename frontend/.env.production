# Browser requests use same-origin /api by default. The Next.js server proxies
# them to BACKEND_URL (configured on the frontend container).
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
